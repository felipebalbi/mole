// Placeholder for the result-ring memory layout.

#let ring-layout = box[
  // TODO: bytes-on-the-wire diagram of REVISION / CAPTURE /
  // MARK / HALT records.
  text(fill: muted, style: "italic")[result-ring layout --- TODO]
]
